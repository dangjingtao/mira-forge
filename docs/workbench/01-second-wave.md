# Mira Forge Second Wave

The second wave turns the first-wave ledger/dashboard into a safe runtime contract for later agent integrations.

Order:

1. T005 Adapter registry and heartbeat
2. T006 Agent session lifecycle
3. T007 SHA-bound review handoff history
4. T008 Dispatch readiness and dependency gate

This wave deliberately stays provider-neutral. OpenCode, Codex, Pi Agent and GitHub-specific behavior belong behind adapters after these contracts are verified.
